<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diploma.khaiid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".signUp.SignUpMenuActivity"
            android:label="Sign up menu"
            android:screenOrientation="portrait" />
        <activity
            android:name=".signUp.SignUpStudentActivity"
            android:label="@string/sign_up"
            android:screenOrientation="portrait" />

        <activity
            android:name=".signUp.SignUpTeacherActivity"
            android:label="@string/new_teacher"
            android:screenOrientation="portrait" />
        <activity
            android:name=".signUp.SignUpCourseActivity"
            android:label="@string/new_course"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SearchActivity"
            android:label="@string/search"
            android:screenOrientation="portrait"/>
    </application>

</manifest>